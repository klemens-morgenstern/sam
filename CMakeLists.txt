# Generated by `boostdep --cmake asem`
# Copyright 2020 Peter Dimov
# Distributed under the Boost Software License, Version 1.0.
# https://www.boost.org/LICENSE_1_0.txt

cmake_minimum_required(VERSION 3.5...3.16)

project(boost_asem VERSION "${BOOST_SUPERPROJECT_VERSION}" LANGUAGES CXX)

add_library(boost_asem STATIC src/src.cpp)
add_library(Boost::asem ALIAS boost_asem)

if (BOOST_ASEM_STANDALONE)
  add_compile_definitions(BOOST_ASEM_STANDALONE=1)
  include_directories(${CMAKE_CURRENT_SOURCE_DIR}/include)
else()
  target_include_directories(boost_asem INTERFACE include)
  target_link_libraries(boost_asem
    INTERFACE
      Boost::asio
      Boost::system
  )
endif()

if(BUILD_TESTING AND EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/test/CMakeLists.txt")
  enable_testing()
  add_subdirectory(test)
endif()

if(BUILD_EXAMPLES AND NOT BOOST_ASEM_STANDALONE AND EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/example/CMakeLists.txt")
  add_subdirectory(example)
endif()

